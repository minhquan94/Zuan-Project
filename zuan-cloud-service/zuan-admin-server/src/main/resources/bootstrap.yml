spring:
  application:
    name: cloud-admin
  cloud:
    config:
      uri: http://localhost:8700
      cloud-admin:
        production:
          eureka:
            instance:
              hostname: localhost
              preferIpAddress: false